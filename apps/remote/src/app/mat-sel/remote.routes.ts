import { Routes } from '@angular/router';
import { MatSelComponent } from './mat-sel.component';

export const REMOTE_ROUTES: Routes = [
  {
    path: 'select',
    component: MatSelComponent,
  },
];
